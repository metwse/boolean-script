# Boolean Script Utility
This module contains Boolean script utilities.

You can build utilities in this module using syntax `make bin/<utility_name>`,
i.e. `make bin/dnf`.

### bnf
Dumps current grammar to `stdout` in BNF format.

Usage: `bin/bnf`
